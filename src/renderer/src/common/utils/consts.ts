export const appDisplayName = Object.freeze({
  dashboard: "Dashboard",
  "balance history": "Balance History",
  "financial request": "Financial Request"
});

export const paymentStatus = Object.freeze({
  APPROVED: 1,
  REJECTED: 2,
  PENDING: 3
});
